import React from 'react'

const Loader = ({title}) => {
  return (
    <div className="loading w-full flex justify-center items-center flex-col">
        <span>Loading</span>
    </div>
  )
}

export default Loader